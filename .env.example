# ===========================================
# Glomun - Variables de entorno
# Copiá este archivo a .env.local y completá los valores
# ===========================================

# MongoDB (requerido para suscripciones, webhooks, contabilidad)
# Obtener URI desde: https://www.mongodb.com/cloud/atlas (MongoDB Atlas)
# Formato: mongodb+srv://<usuario>:<contraseña>@<cluster>.mongodb.net/<nombre_db>?retryWrites=true&w=majority
MONGODB_URI=mongodb+srv://usuario:password@cluster.mongodb.net/glomun-panel?retryWrites=true&w=majority

# Opcional: nombre de base de datos (por defecto se usa el de la URI)
# MONGODB_DB=glomun-panel

# Mercado Pago (suscripciones)
MERCADO_PAGO_ACCESS_TOKEN=APP_USR-xxxx
MERCADO_PAGO_WEBHOOK_SECRET=optional

# URL base del sitio (para redirects y webhooks)
# En desarrollo: http://localhost:6120
# En Vercel: se usa VERCEL_URL automáticamente
APP_BASE_URL=http://localhost:6120

# Resend (emails: magic link para login)
# Obtener API key en https://resend.com
RESEND_API_KEY=re_xxxx
# Opcional: email remitente (por defecto onboarding@resend.dev)
# RESEND_FROM_EMAIL=Glomun <noreply@tudominio.com>

# Admin /admin92 (Basic Auth)
ADMIN_BASIC_USER=admin
ADMIN_BASIC_PASS=tu_contraseña_segura
